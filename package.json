{
  "name": "sarmal",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "fix": "pnpm -r format && pnpm -r lint"
  },
  "packageManager": "pnpm@9.15.0"
}
