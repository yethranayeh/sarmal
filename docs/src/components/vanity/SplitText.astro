---
interface Props {
	text: string;
	className?: string;
}
const { text, className } = Astro.props;
---

{
	text.split("").map((c, i) => (
		<span style={`--i: ${i}`} class={className}>
			{c}
		</span>
	))
}
