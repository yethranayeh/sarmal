---
interface Props {
	label: string;
}

const { label } = Astro.props;
---

<div class='flex items-center gap-2'>
	<span class='w-2.5 h-2.5 bg-primary inline-block shrink-0'></span>
	<span class='font-mono text-[10px] tracking-[0.2em] uppercase text-muted'>{label}</span>
</div>
