---
import BaseLayout from "../layouts/BaseLayout.astro";

import HeroSection from "../components/feature/HeroSection.astro";
import LiveDemoContainer from "../components/feature/demo/LiveDemoContainer.astro";
import SizeSection from "../components/feature/SizeSection.astro";

import { PACKAGE_SIZE, SEPARATOR_DOT } from "../variables";
---

<BaseLayout
	title={`Sarmal ${SEPARATOR_DOT} Animated Curves for Hero Sections, Loading States & Ambient Motion`}
	description={`Animated curves for hero sections, loading states, and ambient motion. ${PACKAGE_SIZE}KB, zero dependencies. A curve, a color, two lines of code.`}
	ogTitle="Sarmal | Animated Curves for React & JavaScript"
>
	<script
		is:inline
		slot="head"
		type="application/ld+json"
		set:html={JSON.stringify({
			"@context": "https://schema.org",
			"@type": "SoftwareApplication",
			name: "Sarmal",
			applicationCategory: "DeveloperApplication",
			operatingSystem: "Any",
			description: `Animated curves for hero sections, loading states, and ambient motion. ${PACKAGE_SIZE}KB, zero dependencies. A curve, a color, two lines of HTML.`,
			url: "https://sarmal.art",
			offers: {
				"@type": "Offer",
				price: "0",
				priceCurrency: "USD",
			},
		})}
	/>
	<main class="w-full space-y-4 md:space-y-16">
		<HeroSection />

		<LiveDemoContainer />

		<SizeSection />
	</main>
</BaseLayout>
